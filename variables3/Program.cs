// Nullable Types
// Nullable types are used when a value type needs to represent the absence of a value (null).

// "int?": A nullable integer type, which can hold any valid int value or null.
int? nullableInt = null;
Console.WriteLine(nullableInt);
// ---------------------------------------------------------------------------